# graphql-devc

GraphQL Server Demo for Facebook DevC Malang April Event

## Run This Project

* `npm install`
* `docker-compose up -d`
* create database `warcraft` in mongo
* `npm run seed`
* `npm start`

## Test

`npm test`
